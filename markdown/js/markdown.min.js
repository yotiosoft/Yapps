const input=document.getElementById('markdown-input');const preview=document.getElementById('preview');function updatePreview(){const markdownText=input.value;preview.innerHTML=marked.parse(markdownText);}
function fitPreviewHeight(){console.log('fitPreviewHeight called');preview.style.height='auto';preview.style.height=input.style.height;}
input.addEventListener('input',updatePreview);new ResizeObserver(fitPreviewHeight).observe(input);updatePreview();
