{
  "scripts": {
    "generate:related-apps": "node scripts/generate-related-apps.mjs",
    "generate:sitemap": "node scripts/generate-sitemap.mjs",
    "generate": "npm run generate:related-apps && npm run generate:sitemap"
  },
  "dependencies": {
    "whois-parsed": "^1.0.14"
  }
}
